# ============================================
# Ejym SaaS - Configuracao de Ambiente
# ============================================
# Copie este arquivo para .env.local e preencha os valores
# NUNCA commite o arquivo .env.local no git!

# ============================================
# FRONTEND (Variaveis VITE_ - expostas no navegador)
# ============================================
# Estas sao PUBLICAS por design - a seguranca vem via Row Level Security (RLS)
VITE_SUPABASE_URL=https://seu-projeto.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# WuzAPI URL para conexao WhatsApp (opcional)
VITE_WUZAPI_URL=https://seu-servidor-wuzapi.com

# ============================================
# CLI/DEPLOY (NAO expostas no frontend)
# ============================================
# Token para deploy de Edge Functions via Supabase CLI
# Gere em: https://supabase.com/dashboard/account/tokens
SUPABASE_ACCESS_TOKEN=sbp_xxxxxxxxxxxxxxxxxxxx

# ============================================
# EDGE FUNCTIONS (Configurar via Supabase Secrets)
# ============================================
# Estes tokens NAO devem estar no .env.local
# Configure-os diretamente no Supabase:
#
# npx supabase secrets set MAILERSEND_API_TOKEN=mlsn.xxxxx
# npx supabase secrets set MAILERSEND_FROM_EMAIL=noreply@seudominio.com
# npx supabase secrets set MAILERSEND_FROM_NAME=Ejym
# npx supabase secrets set WUZAPI_ADMIN_TOKEN=seu-token-admin
# npx supabase secrets set WUZAPI_URL=https://seu-servidor-wuzapi.com
# npx supabase secrets set ASAAS_API_KEY=$aact_xxxxx
# npx supabase secrets set ASAAS_ENVIRONMENT=sandbox

# ============================================
# IMPORTANTE: Seguranca
# ============================================
# - Variaveis VITE_* sao incorporadas no JavaScript e visiveis no navegador
# - Use VITE_* APENAS para dados publicos (URLs, chaves anonimas)
# - NUNCA coloque tokens de API, senhas ou chaves secretas com prefixo VITE_
# - Tokens sensiveis devem ir para Edge Functions via Supabase Secrets
